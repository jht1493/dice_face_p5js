_notes.txt

# --
2022-05-04 10:55:33

>> how to avoid flash??

>> try show all faces

a_ui.canvas_data_chk

bin/install_js-p5live.sh
No on connect in https://p5livemedia.itp.io/p5livemedia.js

https://raw.githubusercontent.com/vanevery/p5LiveMedia/master/public/p5livemedia.js

    constructor(sketch, type, elem, room, host, videoBitrate = null, audioBitrate = null) {


>> 320x240 canvas on face device to get 640x480 in stream out

function create_media_pane(device, vis_in) {
  let capture = device.capture;

  function ready() {
    return capture.loadedmetadata;
  }

function init_media_panes() {
  // First media pane is canvas
  a_media_panes = [
    {
      label: 'Canvas',
      capture: my_canvas,
      ready: function () {
        return 1;
      },

    inst.input = media.capture;
  let media = a_media_panes[imedia];

# --
[] restart of tile host restarts face clients
  tile: Hello

  ui_chat_receive('', id);
  let obj = { name: id, text: 'Bye' };

function ui_chat_receive(str, id) {
  // !!@ tile: Hello restart


[] fix aspect ratio

skt/dice_faces/eff/eff_tile.js
    this.xstep = Math.floor(this.twidth / this.ncell);
    this.ystep = Math.floor(this.theight / this.ncell);

NO media for init imedia 2

[] reset button on live
[] reset timer if too many 
  NOT media.ready imedia 2

>> Not reliable - need to reset on device
  after long play
  
https://editor.p5js.org/jht1493/sketches/P_J51He7l
shadertoy

https://editor.p5js.org/jht1493/sketches/JMzjqwJQb
BLM shadertoy